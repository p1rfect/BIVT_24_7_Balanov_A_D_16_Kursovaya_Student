{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run Student FastAPI Server",
            "type": "shell",
            "command": ".\\.venv\\Scripts\\python.exe -m uvicorn main:app --host 127.0.0.1 --port 8000 --reload",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": []
        }
    ]
}
